# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RocketApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RocketApp
  pod 'SDWebImage', '~> 4.0'
  pod 'SnapKit', '~> 4.0.0'
  pod 'Alamofire', '~> 4.7'
  pod 'SwiftLint'
  pod 'FlagKit'

  target 'RocketAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RocketAppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
